This is a Vala version of Douglas Crockford's jsmin 
(http://www.crockford.com/javascript/jsmin.html).

It's simple to compile:

	user@machine:~$ valac --pkg gio-2.0 -o jsmin jsmin.vala
	
On Windows:

	valac -D WINNT --pkg gio-2.0 -o jsmin jsmin.vala
	
and simple to use:

	user@machine:~$ jsmin input.js
	
will generate input-min.js

or

	user@machine:~$ jsmin input.js output.js
	
will of course generate output.js
